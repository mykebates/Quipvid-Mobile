<?xml version="1.0" encoding="UTF-8"?>

<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:QuipVid.Views"
       Title="QuipVid"
       x:Class="QuipVid.AppShell">
    <Shell.Resources>
        <ResourceDictionary>
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <FlyoutItem>
        <Tab>
            <ShellContent Title="Browse" Icon="tab_feed.png" Style="{StaticResource DefaultShellStyle}">
                <views:ItemsPage />
            </ShellContent>
        </Tab>
    </FlyoutItem>
    <FlyoutItem>
        <Tab>
            <ShellContent Title="About" Icon="tab_about.png" Style="{StaticResource DefaultShellStyle}">
                <views:AboutPage />
            </ShellContent>
        </Tab>
    </FlyoutItem>

</Shell>