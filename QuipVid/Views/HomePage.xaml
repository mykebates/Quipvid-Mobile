<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:QuipVid.Controls;assembly=QuipVid"
             x:Class="QuipVid.Views.HomePage"
             x:Name="BrowseItemsPage"
             BackgroundColor="{StaticResource SecondaryColor}">
    <Shell.SearchHandler>
        <controls:QuipSearchHandler />
    </Shell.SearchHandler>
    <Shell.TitleView>
        <controls:Logo />
    </Shell.TitleView>
    <ScrollView>
        <StackLayout Padding="0,0,0,40" Spacing="20">
            <controls:QuipCarousel x:Name="QuipCarousel" />
            <controls:HorizontalCardSet />
            <controls:HorizontalCardSet />
            <controls:HorizontalCardSet />
        </StackLayout>
    </ScrollView>
</ContentPage>
